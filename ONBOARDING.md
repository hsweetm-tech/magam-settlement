# 마감 정산 시스템 — 개발 인수인계 (ONBOARDING)

> **이 파일이 인수인계서 정본(source of truth)입니다.** 코드와 함께 git으로 따라다니므로,
> 어디서 이어받든 이 파일만 읽으면 됩니다. (구버전 `G:\내 드라이브\정산시스템\HANDOVER_magam-settlement.md`는 폐기 — 더 갱신 안 함.)
>
> **새 세션 시작 한 줄(복붙):**
> `magam-settlement 일일정산 시스템 작업. ONBOARDING.md 먼저 읽고 이어서 해.`
>
> **웹(클라우드) vs 로컬 차이 — 중요:**
> - **Claude Code 웹**(claude.ai/code 등): 클라우드 일회용 컨테이너에서 실행. GitHub clone한 코드+네트워크만 있음. **사장님 PC의 `G:\`·`D:\` 등 로컬 디스크는 접근 불가.** 코드 작업·푸시는 GitHub 통해 가능.
> - **Claude Code 로컬**(PC에 설치): 안티그래비티처럼 PC에서 직접 실행 → `G:\`(구글드라이브 동기화 폴더)·`D:\` 파일 직접 읽기/쓰기 가능. 로컬 데이터·드라이브를 다뤄야 하면 로컬로 실행할 것. (설치는 §7 참고)
>
> 최종 업데이트: 2026-06-15

## 1. 한 줄 요약
식당(매장) **일일/월 정산** 단일 파일 정적 HTML 앱. 백엔드 없음. 동업자 간 공유.
판매-only 일일 대사 도구에서 풀 정산 시스템으로 확장 중.

## 2. 이 저장소에 든 것 / 안 든 것
| 든 것 | 안 든 것 (의도적 제외) |
|---|---|
| `일일정산.html` — 앱 전체 (단일 파일) | `마감영수증/` `매입영수증/` — 영수증 폰 사진 |
| `ONBOARDING.md` — 이 문서 | `정리/` — 일별 정산 JSON·색인·원본보관 (실제 재무 원장) |
| `.gitignore` | `*_backup_*.html` — 백업 (git 히스토리가 대체) |

⚠️ **재무·영수증 데이터는 git으로 동기화되지 않습니다.** 민감 정보라 GitHub에 올리지 않음.
코드만 PC 간 동기화됨. 실제 운영 데이터를 여러 PC에서 공유하려면 별도 방법(GAS 클라우드/직접 복사) 필요 — 미결 사항.

## 3. 아키텍처 (확정)
- 단일 파일 정적 HTML, 백엔드 없음. `localStorage` + 선택적 Google Apps Script 클라우드 백업.
- **디스크 파일이 원장(진짜 기준).** 앱(`일일정산.html`)은 그걸 불러와 보는 뷰어.
- **OCR = Claude Code 채팅에서 처리** ("오늘 정리해줘" → 색인·엑셀·import.json 생성, 원본은 `원본보관/`에 라벨링 이동). **API 키·크레딧·클라우드 불필요.**
  - (참고: 과거 브라우저 직결 Anthropic/Tesseract OCR 시도는 폐기됨 — 크레딧 없음/한글 영수증 품질 한계. 현 방식으로 해결됨.)

## 4. 핵심 고정 결정사항
- **손익 분배 = 정률 단순분배** (동업자별 고정 비율, 합 100%, 설정 가능).
- **매출 기준 = 마감영수증(일일 정산 내역)**. 상품별 매출은 메뉴 분석용.
- **현금 30만원 = 시재 플로트(petty cash)** — 매출/손익에 안 섞음. 실제 현금사용만 비용. 10만선 도달 시 보충 이벤트 기록.
- **컴프(내부자 할인·서비스) = 공동 비용 처리** 후 잔여를 정률 분배. 귀속자·내용은 색인에 항상 기록(투명성).
- 영수증 = 폰 사진(매일 전송), 통장 = 엑셀.
- 공유 = GAS 클라우드 동기화 AND 월말 리포트 export.
- 큰 수정 전 `일일정산_backup_YYYYMMDD.html` 백업 → **이제 git 히스토리가 대체**.
- 세무: 법적 1순위는 적격증빙(세금계산서·카드전표·현금영수증, 홈택스 전자). 이 사진 아카이브는 **대체 아닌 보조**(소명·내부 정산 증적).

## 5. 진행 상태

### Phase 1 = 완료 · 검증됨 ✅
매입·비용 섹션, 비용 분류, 통장 엑셀 파서 + 입출금 자동분류 + 대사 검증,
일 손익(매출−매입·비용, 매출세액−매입세액=납부세액), record/draft 스키마 마이그레이션.
(검증: headless Chrome — SYNTAX_OK + 런타임 + A/B 무회귀)

### Phase 2 = 완료 ✅ (2026-05-20)
- **1-A 월정산 탭 골격** ✅ (2026-05-18, commit `9bf4301`): 5번째 탭 "월정산" + 월 선택 input, 손익계산서 자리표시자
- **1-B 월 손익계산서 + POS 임포트** ✅ (commit `28db700`): 일자별 record 집계 (매출−매입·비용, 매출세액−매입세액=납부세액)
- **급여 Phase A** ✅ (commit `4bbe63b`): 인건비 입력 섹션 + 일/월 손익 자동 차감
- **급여 Phase B** ✅ (2026-05-20): 급여 행 ↔ 통장 인건비 출금 자동 매칭. 기준: 금액 동일(필수) + 직원명 적요 포함(가산) + 동일 일자(가산). 현금/카드 지급은 매칭 제외. 급여 표 "통장대사" 컬럼(✓/✗/현금)·통장 표 매칭 뱃지(↔ 급여 #N)·검증 패널 "급여 통장 대사" 이슈로 표시.
- **2 정률 분배** ✅ (2026-05-20): 월정산 탭에 "정률 분배 설정" 섹션. 동업자명·비율·메모 편집(로컬 저장). 합계 100%일 때만 분배 계산. "할인·쿠폰(컴프)을 분배 전 공동비용 차감" 토글 — 켜면 분배 대상 = 월손익 − 월할인합계. 손익계산서 아래에 동업자별 분배액 표가 표시됨.
- **3 PDF/엑셀 export** ✅ (2026-05-20): 월정산 탭 헤더에 [엑셀] [PDF] 버튼. 엑셀=4시트(요약·일자별·매입분류·정률분배), XLSX.writeFile. PDF=`window.print()` + `@media print` CSS(헤더·사이드바·다른 탭 숨김) → 브라우저 'PDF로 저장' 사용 (새 라이브러리 의존성 없음).
- **+ 홈택스 적격증빙 대사** ✅ (2026-05-20): 월정산 탭에 "홈택스 적격증빙" 업로드 섹션 — 매입세금계산서·카드매입·현금영수증 엑셀(.xlsx/.csv)을 드래그&드롭. 파일명·컬럼으로 docType 자동 감지(세금계산서/카드전표/현금영수증), 한국식 컬럼명(작성일자·공급가액·세액·합계·등록번호 등)을 다중 변형 패턴으로 매핑. 월별 localStorage 저장. 대사 기준: 사업자번호(정규화 일치) → 거래처+합계 ±10원 → 합계+일자 ±3일. 결과 카드: 일치/홈택스만/입력만 (입력 누락·간이영수증 식별). 엑셀 export에도 '홈택스대사' 시트 포함.

### Phase 3 — 식당 운영 KPI (진행 중 🚧)
- **3-1 식자재 원가율 + Prime Cost** ✅ (2026-05-20): 식당 핵심 KPI 3종(식자재 원가율·인건비율·Prime Cost%)을 분모=매출공급가 기준으로 계산. 업계 임계치(식자재≤35%·인건비≤30%·Prime Cost≤60%) 4단계 분류(ok/normal/warn/danger) 색상 표시. 월정산 탭 + 분석 탭에 KPI 카드 행 노출. 일일 검증 패널에 "운영 KPI" 이슈 추가 (warn/danger 시 알람). `computeCostRatios()` pure 헬퍼, 26 케이스 단위 테스트.
- **3-2 메뉴믹스 ABC + 객단가 추세 + 시간×요일 히트맵** ✅ (2026-05-20): 분석 탭에 새 view 옵션 [메뉴믹스][히트맵] 추가. 메뉴믹스 = Pareto ABC 분류(A≤80% 누적·B 80~95%·C 95~100%) + 메뉴 30개 상세표(판매수·평균객단가·비중·누적·등급). 히트맵 = 7요일×24시간 SVG 셀, 매출 강도 4단계 색, 피크 셀 자동 표시. 객단가 추세 = 일별 라인 차트 + 평균선 + 요일별 평균 mini-bar — view 선택과 무관하게 항상 노출. `computeMenuMix` / `computeHourWeekdayHeatmap` / `computeAvgCheckTrend` pure 헬퍼, 21 케이스 단위 테스트.
- **3-3 손익계산서 + 고정비 자동 반영** ✅ (2026-05-20): 월정산에 "고정비 설정" 카드 추가 — 임대료·관리비·통신비·보험료 등 매월 정액을 1회 입력하면 모든 월에 자동 반영. 단, 해당 월에 같은 분류 매입행이 있으면 매입행이 우선(설정값 무시) → 변동 시 매입·비용으로 덮어쓰기. localStorage `fixed_monthly_expenses`에 저장 + 백업/복원 페이로드 포함. 월정산 운영 KPI 카드 아래에 정식 손익계산서(수직 PnL) 표 추가: 매출액 → (−)매출원가(식자재+주류) → 매출총이익 → (−)판관비(분류별 세부) → 영업이익 → (−)부가세 납부 → 당기순이익(간이). 매출비(%) 컬럼 + 적용된 고정비 풋노트. 엑셀 export에 '손익계산서' 시트 추가. `resolveFixedExpensesForMonth` + 확장된 `computeMonthlyData` (bySupplyCategory) + `renderIncomeStatement` 추가. 14 케이스 단위 테스트.
- **3-4 월정산 탭 sub-tab 분리** ✅ (2026-05-20): 월정산 탭이 너무 길어서 [손익][분배][증빙][설정] 4-sub-tab으로 분할. 월 선택·엑셀·PDF 버튼은 공통 헤더. 손익 = KPI/운영KPI/손익계산서/일자별/매입분류. 분배 = 동업자 비율 설정 + 결과. 증빙 = 홈택스 업로드 + 대사. 설정 = 고정비. PDF 인쇄 시 자동으로 손익 sub-tab으로 전환 후 인쇄 (@media print에서 msub-profit만 노출). `renderMonthlyReport`를 `renderMonthlyProfitContent` + `renderMonthlyDistContent` + `renderHometaxReconciliation` 3개로 분할. `switchMonthlySubTab(name)` 추가.
- **3-5 환경 변수 분석 (날씨 + 공휴일)** ✅ (2026-05-21): Open-Meteo API(무료·키 불필요, 강남역 좌표 37.4979/127.0276) 연동해 일별 매출과 환경 변수의 영향 분석. 마감 시 그날 날씨 자동 fetch + 분석 탭 [날씨 가져오기] 버튼으로 전체 backfill (과거=archive API, 오늘+미래=forecast API, 경계 자동 처리). 분석 탭에 "환경 변수 분석" 카드 3구획: ① 요일/공휴일(평일/주말/공휴일/대체공휴일 평균 매출), ② 강수(맑음/흐림 vs 비/눈), ③ 기온(추움<5°, 쌀쌀 5~15°, 쾌적 15~25°, 더움 25~30°, 폭염>30°) — 모두 막대 바 시각화. 이상기상(폭우·폭설·천둥번개) 일자 별도 표. 한국 공휴일 2026~2027 하드코딩(대체공휴일 포함). `getHoliday` / `getDayType` / `isRainDay` / `computeWeatherAnalysis` pure 헬퍼, 28 케이스 단위 테스트.
- **3-6 손익 추정 (Profit Forecast)** ✅ (2026-05-21 초안, 5-21 개편): **별도 메인 탭 [손익 추정]**으로 분리됨. 월 단위 분석 — 이번 달(또는 선택 월) 1일~말일 모두 일별로 표시. 실제 매출(누적) + 잔여 일수 추정 = 월 예상 매출. 매출 모델: 요일별 평균(전체 학습 records, outlier·휴무 제외) × 비/눈 0.85 × 공휴일 0.55 × 화요일 휴무=0. 미래 날씨는 Open-Meteo forecast(오늘~월말). 비용: 변동비=매출공급가×원가율, 인건비·기타고정비=월 전액(안분 X). 일별 막대 차트(실제=검정·이벤트=주황·추정=옅음·휴무/미마감=회색·오늘 표식) + 범례 + 일별 상세 표. **Outlier 자동/수동 제외**: 영업일 매출 median × 3 초과는 자동, 마감내역 [이벤트 표시] 버튼으로 수동 토글(record._meta.excludeFromForecast). `computeMonthlyForecast` / `_detectForecastOutliers` / `_computeWeekdayBaseline` pure 헬퍼, 28 케이스 단위 테스트.
- **3-7 이지피지 PG 정산 대사 (대사 ①②)** ✅ (2026-06-11, main 병합·Pages 배포 완료): 월정산 탭에 [PG정산] sub-tab 추가. 이지피지 **PG정산상세내역** 엑셀(.xlsx) 업로드 → 월별 localStorage `pg_settle_{YYYY-MM}` 저장(백업/복원·클라우드 동기화 페이로드 포함). 컬럼은 다중 변형 패턴으로 자동 매핑(지급일자·거래일자·정산대상금액·대행수수료·대행수수료VAT·실지급금액·발급사 등), 날짜는 YYYYMMDD/엑셀시리얼 정규화. **대사 ①** 지급일자별 `실지급금액` 합계 ↔ 통장 "에비뉴에이치" 입금(`getKbBankData`) — 같은 날짜+금액 우선 매칭 → 금액 일치 ±3일 → 날짜 일치(금액 차액). 일치/차액/입금누락/익월 정산예정(선택월보다 늦은 지급일=pending) 분류. **대사 ②** 거래일자별 `정산대상금액`(카드매출) ↔ 일일 마감 카드매출(record cardRows 합 우선, 없으면 totals.posCard). `마감>PG`=PG 외 채널(삼성 직승인·해외카드 VAN) 추정으로 정상 가능, `PG>마감`=마감 과소입력 의심으로 강조. (참고: 5월 실데이터 검증 — 8개 정산회차 전건 원단위 일치, 6월 정산예정 3건.) `parsePgSettleRows` / `reconcilePgBank` / `reconcilePgDailyCard` / `_recordCardSales` / `_parsePgDate` pure 헬퍼, 25 케이스 단위 테스트(`.test-pg-reconcile.js`). 백업 round-trip 테스트에 pgSettle 케이스 추가.
- **3-8 직승인(PG 외 카드) ↔ 통장 직접입금 역산 (대사 ③)** ✅ (2026-06-15): 이지피지를 안 거치고 카드사가 통장에 직접 넣어주는 정산(삼성 직승인·해외카드 Visa/Master는 하나·BC 등 매입사가 정산)을 통장 직접입금과 대조. 어려운 이유 — (a) 건별 수수료 상세 파일 없음, (b) 같은 카드사가 PG·직승인 양쪽에 섞임. **방식(사장님 승인): 통장 직접입금액으로 수수료 역산.** `수수료율=(VAN매출−입금)÷매출`, `역산매출=입금÷(1−율)`. 매출 기준 = 이지피지 **매출내역 xlsx의 VAN 시트**(삼성 3건 140,400 / 해외 5건 322,900). **구현 내용**: PG정산 sub-tab에 "VAN 직승인 매출" 업로드 추가 → `parseVanSalesRows`가 VAN 시트의 발급사별 '○○ 합계' 행(거래건수·거래금액)을 자동 파싱, 월별 localStorage `pg_van_{YYYY-MM}` 저장. 대사 ③: 통장 입금 중 PG(에비뉴=대사①)·노이즈(쿠팡/쿠페이 취소환입·1원 인증) 제외하고 `DIRECT_CARD_ISSUERS` 규칙으로 발급사 분류(삼성→/삼성/, 해외→/하나|비씨|BC|비자|마스타|VISA|MASTER|해외/), 발급사별 입금합으로 `_reverseFeeRate` 역산. 상태: 0.5~4%=ok / 율<0.5%=over(입금>매출 ⚠) / 율>4%=partial(부분·익월 정산 ⏳). 입금 상세표에 역산매출(입금÷(1−율)) 표시, [역산율 저장] 버튼 → `pg_direct_rates`에 발급사별 율 누적(다음달 `매출×(1−율)=예상입금` 대조용). VAN에 없는데 카드사로 분류된 입금은 orphan으로 별도 경고. 발급사별 율 [역산율 저장]→`pg_direct_rates`. 백업 payload에 pgVan·pgDirectRates 포함.
  - **⚠️ 처음엔 '카드사별 마감−PG=직승인'으로 시도했다가 폐기**(2026-06-15): 실데이터로 돌려보니 **마감 카드매출의 카드사 칸이 OCR로 자주 뒤섞여 있어**(아래 3-9 참고) 카드사별로는 마감≠이지피지라 직승인을 마감에서 못 뽑음. **직승인 매출의 정답 기준은 마감이 아니라 이지피지 VAN 시트.** 그래서 ③은 VAN 기준 유지.
- **3-9 마감 카드매출 카드사 OCR 교정 (대사 ④)** ✅ (2026-06-15): **핵심 발견 — 일일 마감 cardRows의 카드사 칸이 OCR로 자주 오분류됨.** 예) 5/15 마감 총액 4,170,300 = 이지피지 총액과 정확히 일치하지만 삼성 +1,810,700·신한 −1,641,200 등 금액이 엉뚱한 카드사 칸에 들어감. **마감 카드사별은 신뢰 불가, 이지피지 PG정산상세내역(건별 정확)이 정답.** 구현: PG정산 sub-tab ④ 섹션. `reconcileCloseVsPg`가 일자·카드사별로 마감 cardRows vs 이지피지(거래일자 기준) 대조 → `match`(완전일치) / `mislabel`(총액 같고 카드사만 뒤섞임=OCR오류, **안전 교정 가능**) / `amount_diff`(총액 차이=직승인 또는 입력오차, 자동교정 안 함 표시만) / `no_close`. `applyCloseCorrection(date)`=그날 cardRows를 이지피지 카드사별로 덮어씀(총액 보존), [교정]/[일괄 교정] 버튼. `_matchCardIssuer`(국민카드/국민/하나92751350 등 정규화)·`_displayCardName`·`_pgGrossByDayIssuer` 추가. **5월 실데이터 분류: 일치 8일 / mislabel 7일(5/10·15·17·22·28·29·30) / amount_diff 4일(5/9 +200, 5/24 +95,600, 5/25 +187,200, 5/31 +240,900)**. amount_diff의 마감>PG 합(≈524k)은 직승인 총액(VAN 463,300)+입력오차(≈60k)로 분해됨. 단위테스트 56개 통과, 헤드리스 무회귀.
- **데이터 정정**: 2026-05-09 신한 108,800→108,600(+200 오타)은 이제 대사 ④에서 amount_diff로 자동 표시됨. **드라이브 라벨링_정리본/2026-05-09.json은 로컬에서 직접 108,600으로 수정 완료**(cardRows 합 562,200=posCard). 사장님 브라우저 데이터도 수정됨.
- **3-10 홈택스 품목명 + 단지푸드형 N:1 매칭** ✅ (2026-06-15): 홈택스 세금계산서 목록에 `품목명` 컬럼 있음("수트(4 set)" 등) → `HOMETAX_COLUMNS.item`(품목일자/규격 등 오매칭 방지 구체후보)로 파싱, 대사 그리드 '홈택스 전체'·'홈택스에만 있음' 표에 **품목 컬럼** 표시. `reconcileHometax`에 **N:1 패스** 추가: 같은 거래처(사업자번호/상호 정규화) 입력 N건 합이 홈택스 1건 합계(±10+건수)와 맞으면 묶어 매칭(단지푸드 등 월정산 거래처). 그리드에 'N:1 N건 합산' 뱃지. ⚠️ 홈택스 파서 버그도 같이 수정: 상호에 사업자번호가 들어가던 문제(`공급자사업자등록번호`가 `상호`보다 앞 컬럼인데 vendor 후보 '공급자'가 먼저 매칭) → bizNo 등 구체필드 먼저 매핑+used 컬럼 제외 후 vendor 매핑. 코카콜라 미매칭도 이걸로 해결.
- **3-11 마감 시 구글드라이브 직접 쓰기 (File System Access API)** ✅ (2026-06-15): **웹앱은 브라우저 제약으로 G:\에 못 써서** 마감하면 다운로드 폴더로만 떨어지고 드라이브 원본(라벨링_정리본)이 안 바뀌던 문제 해결. 클라우드 모달 → **[드라이브 폴더 연결]**로 `showDirectoryPicker`로 라벨링_정리본 폴더 1회 선택·권한 허용(핸들 IndexedDB 저장). 이후 마감 시 `writeRecordToDrive`가 그 폴더의 `YYYY-MM-DD.json`을 직접 갱신(기존 salesMemo 등 보존 병합), 미연결/미지원(Safari·iOS)이면 다운로드 폴백. **[전체 드라이브 동기화]**로 모든 record 일괄 백필. **PC Chrome/Edge 전용**(폰·아이패드는 다운로드/클라우드). ⚠️ G:\는 구글드라이브 데스크톱 가상폴더라 실제 쓰기 동작은 사장님 환경에서 1회 검증 권장.
- **3-12 매입 3-way 점검 보드 + 일괄등록/정기/예외/연결** ✅ (2026-06-16): 월정산에 **[매입점검] sub-tab** 신설. 핵심 목적 = **매입을 빠짐없이 잡아 정확한 손익**. `reconcilePurchase3Way(month)`가 거래처별로 **매입(앱 purchaseRows + 고정비 설정) ↔ 세금계산서(홈택스, 카드·현금영수증 제외) ↔ 통장 출금(카드출금 제외)** 셋을 묶어 상태 분류: `complete`(셋 다)·`unpaid`(계산서○ 통장X=외상)·`no_tax`(통장○ 계산서X=간이/카드)·`missing_buy`(계산서/통장○ 매입X=🔴)·`pending`(매입만)·`fixed`(고정비). **거래처 연결**: 사업자번호 우선 → 정규화 상호(`_canonVendor`: (주)/주식회사/㈜ 제거) → 공통 부분문자열 ≥3(`_shareCore`, 통장 약칭 '장서아_코카콜라'↔'코카콜라음료') → 홈택스 대표자명 alias(통장 '김상균'→더월드키친). 고정비-only↔세금계산서-only 금액일치 병합(임대료↔이재선외). **3가지 버튼/매핑**(localStorage `p3w_vendor_map`, 백업 포함): ① **[점검제외]**(kind:fixed) = 점검에서만 숨김, **손익 영향 없음**(이미 매입/고정비로 반영된 임대료 등 중복표시 제거용). ② **[정기등록]**(kind:auto) = 세무·KT 등 매월 발생건, `autoRegisterRecurring`가 그 달 세금계산서/통장 근거 있고 매입 없으면 **실제 금액으로 매입 자동생성**(멱등, renderMonthlyReport·매입점검 진입 시 실행 → 손익 반영). ③ **[연결]**(kind:alias) = KT('주식회사 케이티'↔'KT통신요금05')처럼 자동 불가 건 수동 연결, **적요 끝 숫자(월) 제거 패턴**으로 저장('kt통신요금')→다음달 06도 자동, [🔗해제]로 되돌림. **[일괄등록]** `bulkRegisterMissingPurchases` = missing_buy 건을 세금계산서/통장 데이터로 매입행 일괄생성(미리보기 확인, 분류 기본 식자재/기타). 각 행에 **적요 표시**(📄 세금계산서 품목명/비고 + 🏦 통장 적요). 홈택스 파서에 `item`(품목명)·`note`(비고) 추가. `.test-3way.js` 30케이스. **⚠️ 사장님 액션**: 홈택스는 품목명 파싱 전 데이터라 **재업로드**해야 품목 표시; 5월 임대료(이재선외+임대인)는 [점검제외], 세무·KT는 [연결]→[정기등록] 필요.
- **3-13 매입점검 등록 분류 자동추천 + 개별등록 분류선택** ✅ (2026-06-17): 일괄·개별 등록 시 분류가 무조건 식자재(세금계산서)/기타(통장)로 들어가던 것 개선. `_guessP3wCategory(text)` pure 헬퍼 신설 — 품목명+거래처명 텍스트의 키워드로 EXPENSE_CATEGORIES 중 추천(주류/음료·통신비·공과금·임대료·세금·공과·배달수수료·소모품·수리/비품·마케팅·식자재; 구체분류 먼저 검사, 식자재 맨뒤, 못 찾으면 null→기본값). **공백 유지**(제거 시 '다이소 주방'→'소주' 오매칭). `_p3wDraftFromRow`가 추천분류를 기본값으로 사용. **개별등록**(`registerOneMissingPurchase`)은 confirm 대신 prompt로 추천분류 보여주며 직접 선택·수정. **일괄등록**(`bulkRegisterMissingPurchases`) 미리보기에 거래처별 추천분류(→ 분류) 표시. `.test-3way.js` 16케이스 추가(총 54 통과), 전체 테스트 무회귀. **+ 매입 등록 시 드라이브 자동 저장**(2026-06-17): 기존엔 개별·일괄·정기등록이 localStorage·GAS클라우드까지만 저장되고 구글드라이브 라벨링_정리본 JSON은 마감/전체동기화 때만 갱신됐음 → `_writeRecordsToDriveIfConnected(records)` 헬퍼 신설, `_registerP3wDrafts`(개별·일괄)·`autoRegisterRecurring`(정기)가 영향받은 날짜 record를 드라이브에 자동 반영(폴더 연결 시만, 미연결이면 조용히 건너뜀·fire-and-forget). `_driveDirHandle` 가드.
- **3-14 매입↔통장 사업자번호 없는 거래처 유사병합** ✅ (2026-06-17): `reconcilePurchase3Way`가 사업자번호 없는 거래처는 정규화 상호가 **정확히 같아야만** 묶여서, 매입 등록명(긴 설명형 '강성국 인테리어 커튼구매시공')과 통장 적요(짧은 약칭 '강성국')가 안 맞으면 따로 떴음(통신요금·전기료도 동일). 세금계산서가 있으면 bizNo로 묶여 문제없지만 **세금계산서 없이 매입+통장만** 있는 건이 누락. → 거래처 그룹핑 후 **bizNo 없는 그룹끼리 `_shareCore(a,b,3)`(3글자+ 공통 부분문자열)로 병합** 패스 추가. 매입(buy) 있는 그룹을 anchor 우선(등록명·분류 보존), 동률이면 긴 이름. 3글자 미만 공통(대한가스↔대한제분)은 오병합 방지로 안 묶음. 한글↔영문(케이티↔KT)·2글자 약칭(한전)은 여전히 [연결] 수동 필요. `.test-3way.js` 5케이스 추가(총 59 통과).
- **3-15 체크카드 건별결제 매입 포착** ✅ (2026-06-17): 3-way 통장출금이 `!/체크카드/.test(summary)`로 **체크카드 결제를 통째 제외**해서, 킨코스코리아·롯데슈퍼·KCP 등 거래처에 직접 나간 체크카드 매입이 안 잡히고 등록도 불가했음(5월 21건). 신용카드 대금 일괄출금(카드사로 나감)만 빼야 하는데 체크카드 건별결제(실제 매입)까지 빠진 게 원인. → 필터를 **`kind!=='카드출금'`만**으로 바꿔 체크카드 포함, 통장행에 `card` 플래그 추가. 상태 로직 재배치: 카드매입(`cardBuy>=buy-10`)은 체크카드 통장출금과 매칭돼도 `card`(정상) 유지(no_tax 경고로 안 바뀌게). 등록 초안은 체크카드 출처면 method=체크카드·docType=카드전표. **실데이터 검증: 킨코스 등 6건 missing_buy로 노출(등록 가능), 다이소·대진 등 기존 카드매입 6건은 card 유지.** `.test-3way.js` 7케이스 추가(총 66 통과).
- **기타 수정 (2026-06-15~16, 전부 배포)**: ① **백업 누락 수정** — 통장(`kb_bank_v1`)·매출목표(`salesGoals_v1`)·목표계산기(`goal_calc_v1`)가 백업/클라우드에 안 담겨 다른 기기서 사라지던 버그 → `collectAppSettings`/`applyAppSettings`에 추가. ② **통장 입금매칭 개선** — PG 정산입금(에비뉴=대사①)·직승인 카드입금(대사③)이 '카드매출 미매칭' 경고로 잘못 뜨던 것 제외(`_classifyDepositChannel`), 입금 매칭의 시스템측을 카드사별 gross가 아니라 **PG 지급일자별 실지급금액**으로 바꿔 뭉친 PG 입금과 1:1 매칭. ③ **홈택스 N:1 부분집합 강화** — 단지푸드 과세 세금계산서(3,712,106)+면세 분리발행 대응(`_htSubsetSumN1`). ④ **통장 출금매칭 증빙** — 월 세금계산서(N:1) 거래처는 '계산서 없음' 오인 안 하게 reconcileHometax 결과 참조('계산서 ✓ 월').
- 다음 예정: 6월 통장으로 ③ 해외 잔액 partial→ok 실검증 + 직승인 분류규칙 보강 · ④ amount_diff 일자 직승인/오차 자동 분해(VAN 일자배분) → BEP 시뮬레이션 / 현금흐름 표 / YoY / 부가세 신고서 / 컴프 행단위 / PG·VAN 대사 엑셀 export

### 인프라 — 클라우드 동기화 (코드 구현됨, 사용자 GAS 셋업 필요)
헤더 "클라우드" 버튼 → 모달의 10단계 가이드 따라 GAS 웹앱 만들고 URL/토큰을 양쪽 PC에 동일 입력.
- 자동 옵션 3가지: 마감 시 자동 업로드 / 마감 시 전체 백업 / 앱 시작 시 자동 풀
- 셋업 안 된 PC에서 처음 켜면 데이터 없음 — **헤더 "백업"/"불러오기"로 JSON 수동 옮김**도 가능
- ⚠️ SECRET_TOKEN은 GAS 코드와 모달 입력란이 **정확히 같아야** 함
- **전체 백업 페이로드(2026-05-20~, 2026-06-16 확장)**: `{exportedAt, records, settings, deletedDates}` — `settings`에 `partner_ratios`·컴프토글·`fixed_monthly_expenses`·`initial_capital_v1`·`hometax_{월}`·`pg_settle_{월}`·`pg_van_{월}`·`pg_direct_rates`·`kb_bank_v1`(통장)·`salesGoals_v1`·`goal_calc_v1`·`p3w_vendor_map`(매입점검 예외/정기/연결) 전부 포함. (`cloud_settings`는 기기별 URL/토큰이라 **의도적 제외**.) 백업/복원·GAS 동기화 시 records와 함께 옮겨감. 구버전 페이로드 호환.
- **실데이터 위치(로컬, 2026-06-16)**: `G:\내 드라이브\정산_백업\전체백업_최신.json`(앱 전체 백업=records+settings+홈택스), `…\정산_백업\통장내역\국민은행 계좌내역 *.xlsx`, `…\정산_백업\홈택스\홈택스 세금계산서 매입내역 *.xls`, `G:\내 드라이브\아스트라\매출\이지피지 *.xlsx`(PG정산상세·VAN 매출내역), `G:\내 드라이브\아스트라\라벨링_정리본\*.json`(OCR 일별 원장). 로컬 Claude는 이걸로 3-way/대사 디버깅 가능.

## 6. ⚠️ 테스트 시 주의 (중요)
이 파일의 인라인 `<script>` 안에 GAS 가이드 문자열로 `<!--` / `<script` 시퀀스가 있어,
테스트 하네스용 `<script>` 태그를 **추가/교체하면** Chrome HTML 토크나이저가 깨짐
(가짜 "Unexpected end of input"). 검증은 이렇게:
1. `new Function(추출script)` 로 문법 검사
2. 원본 vs 백업 `--dump-dom` A/B 비교 (init 부작용: `#dateStatus`가 비어있지 않게 됨)
3. 인라인 `onerror`/`onclick` 프로브 (script 태그 추가 ✗)
4. 순수 함수 단위 격리 테스트
- 스크립트 추출은 **UTF-8**로: `[System.IO.File]::ReadAllText(...)` — PS5.1 `Get-Content` ✗

## 7. 다른 PC에서 시작하는 순서
1. Git + GitHub CLI 설치 (없으면): `winget install Git.Git` + `winget install GitHub.cli`
2. `gh auth login` (브라우저 로그인) → `git config --global user.name "hsweetm"` + `user.email "hsweetm@gmail.com"`
3. `git clone https://github.com/hsweetm-tech/magam-settlement.git` (D 드라이브 등 non-NTFS/외장 경로에 받으면 첫 git 명령 시 `safe.directory` 에러 → `git config --global --add safe.directory <클론한 절대경로>` 한 번 실행)
3-b. **Claude Code 로컬 설치(Windows)** — 로컬로 쓰면 `G:\`·`D:\` 직접 접근 가능(안티그래비티처럼):
   - PowerShell에서 `irm https://claude.ai/install.ps1 | iex` (네이티브 설치) **또는** Node.js 18+ 있으면 `npm install -g @anthropic-ai/claude-code`
   - 첫 실행 시 로그인(Claude Pro/Max 구독 또는 Anthropic API 키 필요)
   - 공식 문서: https://docs.claude.com/en/docs/claude-code
4. 프로젝트 폴더에서 `claude` 실행 → "ONBOARDING.md 읽고 이어서 작업하자"
5. **데이터 가져오기** — 이 저장소엔 코드만 있고 정산 데이터는 없음. 두 가지 방법:
   - **임시(빠름)**: 다른 PC에서 헤더 "백업" → JSON 다운 → 이 PC로 옮긴 뒤 헤더 "불러오기"
   - **자동(권장)**: 헤더 "클라우드" → 모달 가이드 따라 GAS URL/토큰 입력 (양쪽 PC 동일하게)
6. 작업 흐름: 시작 전 `git pull`, 작업·수정 후 `git add -A && git commit -m "..." && git push`
