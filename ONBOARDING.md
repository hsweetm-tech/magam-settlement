# 마감 정산 시스템 — 개발 인수인계 (ONBOARDING)

> **다른 PC에서 이어서 개발할 때:** 이 저장소를 `git clone` → Claude Code 실행 →
> "마감 폴더의 ONBOARDING.md 읽고 이어서 작업하자" 라고 말하면 진행 상황이 그대로 이어집니다.
>
> 최종 업데이트: 2026-05-20

## 1. 한 줄 요약
식당(매장) **일일/월 정산** 단일 파일 정적 HTML 앱. 백엔드 없음. 동업자 간 공유.
판매-only 일일 대사 도구에서 풀 정산 시스템으로 확장 중.

## 2. 이 저장소에 든 것 / 안 든 것
| 든 것 | 안 든 것 (의도적 제외) |
|---|---|
| `일일정산.html` — 앱 전체 (단일 파일) | `마감영수증/` `매입영수증/` — 영수증 폰 사진 |
| `ONBOARDING.md` — 이 문서 | `정리/` — 일별 정산 JSON·색인·원본보관 (실제 재무 원장) |
| `.gitignore` | `*_backup_*.html` — 백업 (git 히스토리가 대체) |

⚠️ **재무·영수증 데이터는 git으로 동기화되지 않습니다.** 민감 정보라 GitHub에 올리지 않음.
코드만 PC 간 동기화됨. 실제 운영 데이터를 여러 PC에서 공유하려면 별도 방법(GAS 클라우드/직접 복사) 필요 — 미결 사항.

## 3. 아키텍처 (확정)
- 단일 파일 정적 HTML, 백엔드 없음. `localStorage` + 선택적 Google Apps Script 클라우드 백업.
- **디스크 파일이 원장(진짜 기준).** 앱(`일일정산.html`)은 그걸 불러와 보는 뷰어.
- **OCR = Claude Code 채팅에서 처리** ("오늘 정리해줘" → 색인·엑셀·import.json 생성, 원본은 `원본보관/`에 라벨링 이동). **API 키·크레딧·클라우드 불필요.**
  - (참고: 과거 브라우저 직결 Anthropic/Tesseract OCR 시도는 폐기됨 — 크레딧 없음/한글 영수증 품질 한계. 현 방식으로 해결됨.)

## 4. 핵심 고정 결정사항
- **손익 분배 = 정률 단순분배** (동업자별 고정 비율, 합 100%, 설정 가능).
- **매출 기준 = 마감영수증(일일 정산 내역)**. 상품별 매출은 메뉴 분석용.
- **현금 30만원 = 시재 플로트(petty cash)** — 매출/손익에 안 섞음. 실제 현금사용만 비용. 10만선 도달 시 보충 이벤트 기록.
- **컴프(내부자 할인·서비스) = 공동 비용 처리** 후 잔여를 정률 분배. 귀속자·내용은 색인에 항상 기록(투명성).
- 영수증 = 폰 사진(매일 전송), 통장 = 엑셀.
- 공유 = GAS 클라우드 동기화 AND 월말 리포트 export.
- 큰 수정 전 `일일정산_backup_YYYYMMDD.html` 백업 → **이제 git 히스토리가 대체**.
- 세무: 법적 1순위는 적격증빙(세금계산서·카드전표·현금영수증, 홈택스 전자). 이 사진 아카이브는 **대체 아닌 보조**(소명·내부 정산 증적).

## 5. 진행 상태

### Phase 1 = 완료 · 검증됨 ✅
매입·비용 섹션, 비용 분류, 통장 엑셀 파서 + 입출금 자동분류 + 대사 검증,
일 손익(매출−매입·비용, 매출세액−매입세액=납부세액), record/draft 스키마 마이그레이션.
(검증: headless Chrome — SYNTAX_OK + 런타임 + A/B 무회귀)

### Phase 2 = 완료 ✅ (2026-05-20)
- **1-A 월정산 탭 골격** ✅ (2026-05-18, commit `9bf4301`): 5번째 탭 "월정산" + 월 선택 input, 손익계산서 자리표시자
- **1-B 월 손익계산서 + POS 임포트** ✅ (commit `28db700`): 일자별 record 집계 (매출−매입·비용, 매출세액−매입세액=납부세액)
- **급여 Phase A** ✅ (commit `4bbe63b`): 인건비 입력 섹션 + 일/월 손익 자동 차감
- **급여 Phase B** ✅ (2026-05-20): 급여 행 ↔ 통장 인건비 출금 자동 매칭. 기준: 금액 동일(필수) + 직원명 적요 포함(가산) + 동일 일자(가산). 현금/카드 지급은 매칭 제외. 급여 표 "통장대사" 컬럼(✓/✗/현금)·통장 표 매칭 뱃지(↔ 급여 #N)·검증 패널 "급여 통장 대사" 이슈로 표시.
- **2 정률 분배** ✅ (2026-05-20): 월정산 탭에 "정률 분배 설정" 섹션. 동업자명·비율·메모 편집(로컬 저장). 합계 100%일 때만 분배 계산. "할인·쿠폰(컴프)을 분배 전 공동비용 차감" 토글 — 켜면 분배 대상 = 월손익 − 월할인합계. 손익계산서 아래에 동업자별 분배액 표가 표시됨.
- **3 PDF/엑셀 export** ✅ (2026-05-20): 월정산 탭 헤더에 [엑셀] [PDF] 버튼. 엑셀=4시트(요약·일자별·매입분류·정률분배), XLSX.writeFile. PDF=`window.print()` + `@media print` CSS(헤더·사이드바·다른 탭 숨김) → 브라우저 'PDF로 저장' 사용 (새 라이브러리 의존성 없음).
- **+ 홈택스 적격증빙 대사** ✅ (2026-05-20): 월정산 탭에 "홈택스 적격증빙" 업로드 섹션 — 매입세금계산서·카드매입·현금영수증 엑셀(.xlsx/.csv)을 드래그&드롭. 파일명·컬럼으로 docType 자동 감지(세금계산서/카드전표/현금영수증), 한국식 컬럼명(작성일자·공급가액·세액·합계·등록번호 등)을 다중 변형 패턴으로 매핑. 월별 localStorage 저장. 대사 기준: 사업자번호(정규화 일치) → 거래처+합계 ±10원 → 합계+일자 ±3일. 결과 카드: 일치/홈택스만/입력만 (입력 누락·간이영수증 식별). 엑셀 export에도 '홈택스대사' 시트 포함.

### Phase 3 — 식당 운영 KPI (진행 중 🚧)
- **3-1 식자재 원가율 + Prime Cost** ✅ (2026-05-20): 식당 핵심 KPI 3종(식자재 원가율·인건비율·Prime Cost%)을 분모=매출공급가 기준으로 계산. 업계 임계치(식자재≤35%·인건비≤30%·Prime Cost≤60%) 4단계 분류(ok/normal/warn/danger) 색상 표시. 월정산 탭 + 분석 탭에 KPI 카드 행 노출. 일일 검증 패널에 "운영 KPI" 이슈 추가 (warn/danger 시 알람). `computeCostRatios()` pure 헬퍼, 26 케이스 단위 테스트.
- **3-2 메뉴믹스 ABC + 객단가 추세 + 시간×요일 히트맵** ✅ (2026-05-20): 분석 탭에 새 view 옵션 [메뉴믹스][히트맵] 추가. 메뉴믹스 = Pareto ABC 분류(A≤80% 누적·B 80~95%·C 95~100%) + 메뉴 30개 상세표(판매수·평균객단가·비중·누적·등급). 히트맵 = 7요일×24시간 SVG 셀, 매출 강도 4단계 색, 피크 셀 자동 표시. 객단가 추세 = 일별 라인 차트 + 평균선 + 요일별 평균 mini-bar — view 선택과 무관하게 항상 노출. `computeMenuMix` / `computeHourWeekdayHeatmap` / `computeAvgCheckTrend` pure 헬퍼, 21 케이스 단위 테스트.
- **3-3 손익계산서 + 고정비 자동 반영** ✅ (2026-05-20): 월정산에 "고정비 설정" 카드 추가 — 임대료·관리비·통신비·보험료 등 매월 정액을 1회 입력하면 모든 월에 자동 반영. 단, 해당 월에 같은 분류 매입행이 있으면 매입행이 우선(설정값 무시) → 변동 시 매입·비용으로 덮어쓰기. localStorage `fixed_monthly_expenses`에 저장 + 백업/복원 페이로드 포함. 월정산 운영 KPI 카드 아래에 정식 손익계산서(수직 PnL) 표 추가: 매출액 → (−)매출원가(식자재+주류) → 매출총이익 → (−)판관비(분류별 세부) → 영업이익 → (−)부가세 납부 → 당기순이익(간이). 매출비(%) 컬럼 + 적용된 고정비 풋노트. 엑셀 export에 '손익계산서' 시트 추가. `resolveFixedExpensesForMonth` + 확장된 `computeMonthlyData` (bySupplyCategory) + `renderIncomeStatement` 추가. 14 케이스 단위 테스트.
- **3-4 월정산 탭 sub-tab 분리** ✅ (2026-05-20): 월정산 탭이 너무 길어서 [손익][분배][증빙][설정] 4-sub-tab으로 분할. 월 선택·엑셀·PDF 버튼은 공통 헤더. 손익 = KPI/운영KPI/손익계산서/일자별/매입분류. 분배 = 동업자 비율 설정 + 결과. 증빙 = 홈택스 업로드 + 대사. 설정 = 고정비. PDF 인쇄 시 자동으로 손익 sub-tab으로 전환 후 인쇄 (@media print에서 msub-profit만 노출). `renderMonthlyReport`를 `renderMonthlyProfitContent` + `renderMonthlyDistContent` + `renderHometaxReconciliation` 3개로 분할. `switchMonthlySubTab(name)` 추가.
- **3-5 환경 변수 분석 (날씨 + 공휴일)** ✅ (2026-05-21): Open-Meteo API(무료·키 불필요, 강남역 좌표 37.4979/127.0276) 연동해 일별 매출과 환경 변수의 영향 분석. 마감 시 그날 날씨 자동 fetch + 분석 탭 [날씨 가져오기] 버튼으로 전체 backfill (과거=archive API, 오늘+미래=forecast API, 경계 자동 처리). 분석 탭에 "환경 변수 분석" 카드 3구획: ① 요일/공휴일(평일/주말/공휴일/대체공휴일 평균 매출), ② 강수(맑음/흐림 vs 비/눈), ③ 기온(추움<5°, 쌀쌀 5~15°, 쾌적 15~25°, 더움 25~30°, 폭염>30°) — 모두 막대 바 시각화. 이상기상(폭우·폭설·천둥번개) 일자 별도 표. 한국 공휴일 2026~2027 하드코딩(대체공휴일 포함). `getHoliday` / `getDayType` / `isRainDay` / `computeWeatherAnalysis` pure 헬퍼, 28 케이스 단위 테스트.
- **3-6 손익 추정 (Profit Forecast)** ✅ (2026-05-21): 분석 탭 view 옵션에 [손익추정] 추가. 미래 N일(기본=이번달 잔여, 7/14/30일 선택) 매출·비용·영업이익 예측. 매출 모델: 요일별 평균(과거 records, 휴무일 제외) × 비/눈 0.85 × 공휴일 0.55 × 화요일 휴무=0. 미래 날씨는 Open-Meteo forecast(최대 16일)로 자동 fetch. 비용: 변동비=추정 매출×원가율, 인건비/기타고정비=월 합×(N일/30). 카드 4종(예상 매출/변동비/고정비/영업이익) + 일별 막대 차트(휴무·공휴일·비 색상 분기) + 일별 상세 표. `_computeWeekdayBaseline` / `computeProfitForecast` pure 헬퍼, 15 케이스 단위 테스트.
- 다음 예정: BEP 시뮬레이션 / 현금흐름 표 / YoY 비교 / 부가세 신고서 양식 / 컴프 행단위 기록 / 미세먼지 추가

### 인프라 — 클라우드 동기화 (코드 구현됨, 사용자 GAS 셋업 필요)
헤더 "클라우드" 버튼 → 모달의 10단계 가이드 따라 GAS 웹앱 만들고 URL/토큰을 양쪽 PC에 동일 입력.
- 자동 옵션 3가지: 마감 시 자동 업로드 / 마감 시 전체 백업 / 앱 시작 시 자동 풀
- 셋업 안 된 PC에서 처음 켜면 데이터 없음 — **헤더 "백업"/"불러오기"로 JSON 수동 옮김**도 가능
- ⚠️ SECRET_TOKEN은 GAS 코드와 모달 입력란이 **정확히 같아야** 함
- **전체 백업 페이로드(2026-05-20~)**: `{exportedAt, records, settings}` — `settings`에 정률 분배 비율(`partner_ratios`), 컴프 차감 토글, 월별 홈택스 적재 데이터(`hometax_{YYYY-MM}`) 모두 포함. 백업/복원·GAS 자동 동기화 시 일별 records와 함께 옮겨감. 구버전 페이로드(`settings` 없음)도 호환.

## 6. ⚠️ 테스트 시 주의 (중요)
이 파일의 인라인 `<script>` 안에 GAS 가이드 문자열로 `<!--` / `<script` 시퀀스가 있어,
테스트 하네스용 `<script>` 태그를 **추가/교체하면** Chrome HTML 토크나이저가 깨짐
(가짜 "Unexpected end of input"). 검증은 이렇게:
1. `new Function(추출script)` 로 문법 검사
2. 원본 vs 백업 `--dump-dom` A/B 비교 (init 부작용: `#dateStatus`가 비어있지 않게 됨)
3. 인라인 `onerror`/`onclick` 프로브 (script 태그 추가 ✗)
4. 순수 함수 단위 격리 테스트
- 스크립트 추출은 **UTF-8**로: `[System.IO.File]::ReadAllText(...)` — PS5.1 `Get-Content` ✗

## 7. 다른 PC에서 시작하는 순서
1. Git + GitHub CLI 설치 (없으면): `winget install Git.Git` + `winget install GitHub.cli`
2. `gh auth login` (브라우저 로그인) → `git config --global user.name "hsweetm"` + `user.email "hsweetm@gmail.com"`
3. `git clone https://github.com/hsweetm-tech/magam-settlement.git` (D 드라이브 등 non-NTFS/외장 경로에 받으면 첫 git 명령 시 `safe.directory` 에러 → `git config --global --add safe.directory <클론한 절대경로>` 한 번 실행)
4. Claude Code 실행 → "마감 폴더의 ONBOARDING.md 읽고 이어서 작업하자"
5. **데이터 가져오기** — 이 저장소엔 코드만 있고 정산 데이터는 없음. 두 가지 방법:
   - **임시(빠름)**: 다른 PC에서 헤더 "백업" → JSON 다운 → 이 PC로 옮긴 뒤 헤더 "불러오기"
   - **자동(권장)**: 헤더 "클라우드" → 모달 가이드 따라 GAS URL/토큰 입력 (양쪽 PC 동일하게)
6. 작업 흐름: 시작 전 `git pull`, 작업·수정 후 `git add -A && git commit -m "..." && git push`
